import { createSlice } from "@reduxjs/toolkit";

const searchSlice = createSlice({
    name: "location",
    initialState: {
        cityName: "",
    },

    reducers: {
        searchLocation

        
    }
})